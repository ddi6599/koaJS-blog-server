const config = {
    // 数据库配置
    database: {
        DATABASE: 'blogsql',
        USERNAME: 'root',
        PASSWORD: '',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config
